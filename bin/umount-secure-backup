#!/usr/bin/env bash

# https://vaneyckt.io/posts/safer_bash_scripts_with_set_euxo_pipefail/
set -Eeuo pipefail

echo -n "Unmounting secure drive"
sudo umount-with-wait /home/paul/secure-backup || true
echo
echo -n "Unmounting s3backer"
sudo umount-with-wait "/run/user/$(id -u)/s3backer"
echo
